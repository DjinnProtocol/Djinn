name: Build Server

on:
  push:
    branches:
      - main-v1
    paths:
      - 'djinn_server/**'

jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Build docker image
      - name: Build Docker image
        run: docker build -f djinn_server/Dockerfile -t djinn_server .
